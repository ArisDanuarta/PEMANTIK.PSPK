import 'package:flutter/material.dart';

class AppColors {
  // === WARNA PRIMER PSPK ===
  static const birNavy = Color(0xFF102E50);
  static const kuningEmas = Color(0xFFF2AF3E);
  static const merahMarun = Color(0xFFA8281C);

  // === WARNA SEKUNDER PSPK ===
  static const jingga = Color(0xFFDF632F);
  static const birTeal = Color(0xFF0874AA);
  static const kuningMuda = Color(0xFFF4B867);

  // === TURUNAN ===
  static const birNavyMuda = Color(0xFFEEF4FA);
  static const birNavyGelap = Color(0xFF0A1F38);

  // === SEMANTIK ===
  static const sukses = Color(0xFF22C55E);
  static const gagal = Color(0xFFA8281C);

  // === NETRAL ===
  static const background = Color(0xFFF9FAFB);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = birNavy; // primary text (dark navy)
  static const textMuted = Color(0xFF6B7280);
  static const border = Color(0xFFE5E7EB);

  // === DESIGN.MD TOKENS ===
  static const primaryContainer = Color(0xFF102E50);
  static const onPrimaryContainer = Color(0xFF7C96BE);
  static const surfaceContainerLowest = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFD3E4FE);
  static const onSurfaceVariant = Color(0xFF43474E);
  static const secondaryContainer = Color(0xFFFEBA48);
  static const onSecondaryFixed = Color(0xFF281800);
  static const outlineVariant = Color(0xFFC4C6CF);
  static const errorContainer = Color(0xFFFFDAD6);
  static const onErrorContainer = Color(0xFF93000A);
  static const primary = Color(0xFF001934);
  static const onPrimary = Color(0xFFFFFFFF);
  static const onSurface = Color(0xFF0B1C30);
}
